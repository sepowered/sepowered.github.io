import { style } from '@vanilla-extract/css';

import { rem } from '@semantic/styles';

export const comment = style({
  marginTop: rem(56),
});

export const divider = style({
  marginBlock: rem(56),
});
